#include <stdio.h>
#include <stdlib.h>
/**
 * main - function that does a lot.
 * @argc: first parameter
 * @argv: second parameter
 * Return: an int
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - 1]);

	return (0);
}
