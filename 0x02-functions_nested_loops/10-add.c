#include "holberton.h"
/**
 * add - adds to numbers and returns the sum
 * @a: a parameter
 * @b: a parameter
 * Return: gives the result of the two integers
 */
int add(int a, int b)
{
	return (a + b);
}
