#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int any;

	for (any = 0; any <= 10; any++)
	{
		printf("%d", any);
	}
	printf('\n');
	return (0);
}
